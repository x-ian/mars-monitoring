<%= form_for(@message_type) do |f| %>
  <% if @message_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@message_type.errors.count, "error") %> prohibited this message_type from being saved:</h2>

      <ul>
      <% @message_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
