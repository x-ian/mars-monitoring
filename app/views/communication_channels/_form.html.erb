<%= form_for(@communication_channel) do |f| %>
  <% if @communication_channel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@communication_channel.errors.count, "error") %> prohibited this communication_channel from being saved:</h2>

      <ul>
      <% @communication_channel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
