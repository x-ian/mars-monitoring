<h1>Listing communication_channels</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @communication_channels.each do |communication_channel| %>
  <tr>
    <td><%= communication_channel.name %></td>
    <td><%= link_to 'Show', communication_channel %></td>
    <td><%= link_to 'Edit', edit_communication_channel_path(communication_channel) %></td>
    <td><%= link_to 'Destroy', communication_channel, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Communication channel', new_communication_channel_path %>
