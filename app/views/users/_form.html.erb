<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :customer_id %><br />
	<%= f.select :customer_id, Customer.accessible_by(current_ability).collect{|p| [p.name, p.id]} %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :mobile_number %><br />
    <%= f.text_field :mobile_number %>
  </div>
  <div class="field">
    <%= f.label :role %><br />
	<%= f.select :role, User::ROLES.collect{|p| [p]} %>
  </div>  
  <div class="field">
	 <%= f.label :time_zone %></br>
	 <%= f.collection_select :time_zone, ActiveSupport::TimeZone.all, :name, :to_s, { selected: f.object.time_zone } %>
 </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
